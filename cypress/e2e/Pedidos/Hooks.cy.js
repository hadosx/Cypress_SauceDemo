/// <reference types='Cypress'/>

    
before(() => {
    //Realizar alguma pré-condição para iniciar os testes - Só executa uma vez
});

beforeEach(() => {
    //Realizar uma pré-condidção para inicar cada teste- Executa todo incio de cada teste
});

after(() => {
    //Realizar uma pré-condição depois que finaliza todos os testes. - Só executa uma vez
});

afterEach(() => {
    //Realizar uma pós condição depois que finaliza cada teste, executa uma vez em cada teste. teste
});